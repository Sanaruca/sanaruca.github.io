{
  "name": "mi-web",
  "version": "1.0.0",
  "description": "",
  "main": "build/index.html",
  "scripts": {
    "build:pug": "pug -O .\\options.js -o .\\build templates/index.pug",
    "build:sass": "sass --style compressed --no-source-map .\\scss\\index.scss .\\build\\stylesheets\\styles.css",
    "build": "npm run build:pug && npm run build:sass",
    "dev": "watch \"npm run build\" -d --ignoreDirectoryPattern=\"/(build|node_modules)/\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "sanaruca",
  "license": "ISC",
  "devDependencies": {
    "pug": "^3.0.2",
    "sass": "^1.53.0",
    "watch": "^1.0.2"
  }
}
